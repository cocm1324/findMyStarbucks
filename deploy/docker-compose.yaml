version: '3.6'

services:
  findmystarbucks.web:
    container_name: findmystarbucks.web
    build: ../front
    ports:
      - "${HTTP_PORT}:${HTTP_PORT}"
    image: findmystarbucks.web:${VERSION}

  findmystarbucks.api:
    container_name: findmystarbucks.api
    build: ../kostack
    ports: 
      - "${API_PORT}:${API_PORT}"
    image: findmystarbucks.api:${VERSION}